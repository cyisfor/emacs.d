(setq edity-types '(text markdown html xml))
(setq programmy-types '(c python lisp lua perl java javascript go lisp))
(defun type->hook (type)
  (intern (concat (symbol-name type) "-mode-hook")))
(provide 'types)
